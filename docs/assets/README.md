# Documentation assets

All DHCPulse screenshots in this directory are captured from the bundled `samples/microsoft-dhcp-realistic-large.xml` configuration. The sample uses documentation address ranges, reserved `.example` names, and generated identifiers. No production or personal data is included.

The social preview asset is composed from the same product screenshot and the repository's existing visual language. Update the screenshots and preview when a release materially changes the primary workflow.
